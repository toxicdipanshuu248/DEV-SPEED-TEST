{
  "name": "dev-speed-matrix",
  "version": "1.0.0",
  "private": true,
  "description": "DEV SPEED MATRIX — internet speed test website (Node.js backend, Render-ready, zero dependencies)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=16"
  }
}
